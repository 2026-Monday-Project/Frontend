import Router from '@/router/Router';
import './App.css';

const App = () => {
    return (
        <Router />
    );
};

export default App;
import Router from "@/router/Router";

const App = () => {
  return <Router />;
};

export default App;
